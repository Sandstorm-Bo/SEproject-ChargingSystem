<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.charging.station.mapper.DispatchRecordMapper">

    <insert id="insertDispatchRecord">
        INSERT INTO dispatch_record (dispatch_id, dispatch_type, detail, car_count)
        VALUES (#{dispatchId}, #{dispatchType}, #{detail}, #{carCount})
    </insert>

    <select id="getAllDispatchRecords" resultType="com.charging.station.domain.DispatchRecord">
        SELECT * FROM dispatch_record ORDER BY created_at DESC
    </select>

</mapper>
